{
  "name": "linkdrop-monorepo",
  "version": "1.0.0",
  "private": true,
  "main": "index.js",
  "repository": "https://github.com/LinkdropProtocol/one-to-one-linkdrop.git",
  "license": "MIT",
  "workspaces": [
    "app",
    "contracts",
    "scripts",
    "server"
  ],
  "scripts": {
    "test": "mocha --require @babel/polyfill --require @babel/register",
    "deploy-mastercopy": "npx babel-node scripts/deploy_mastercopy.js",
    "deploy-factory": "npx babel-node scripts/deploy_factory.js",
    "generate-links": "npx babel-node scripts/generate-links.js",
    "setup-linkdrop": "yarn deploy-mastercopy && yarn deploy-factory && yarn generate-links",
    "claim": "npx babel-node scripts/claim.js",
    "server": "npx babel-node server/server.js"
  },
  "standard": {
    "parser": "babel-eslint"
  },
  "dependencies": {
    "fast-csv": "^2.4.1"
  }
}